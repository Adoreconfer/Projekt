<Window x:Class="Library.Forms.ChangePassword"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Library.Forms"
        mc:Ignorable="d"
        Title="Change Password" Height="320" Width="420" WindowStartupLocation="CenterScreen" Icon="/Forms/chpass.png">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" VerticalAlignment="Center" HorizontalAlignment="Center">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="150"/>
            </Grid.ColumnDefinitions>

            <Label Grid.Row="0" Grid.Column="0" Content="New password:" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,5,10,5"/>
            <PasswordBox x:Name="passBox" Grid.Row="0" Grid.Column="1" Height="25" VerticalContentAlignment="Center" Margin="0,5,0,5"/>

            <Label Grid.Row="1" Grid.Column="0" Content="Confirm password:" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,5,10,5"/>
            <PasswordBox x:Name="changepassBox" Grid.Row="1" Grid.Column="1" Height="25" VerticalContentAlignment="Center" Margin="0,5,0,5"/>
        </Grid>

        <StackPanel Grid.Row="1" HorizontalAlignment="Center" Margin="0,15,0,0">
            <Button x:Name="changePass" Content="Change password" Width="140" Height="28" Margin="0,0,0,8" Click="ChangeBtn"/>
            <Button x:Name="backBtn" Content="Back" Width="85" Height="28" Click="Button_Click"/>
        </StackPanel>
    </Grid>
</Window>
