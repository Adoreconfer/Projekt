<Window x:Class="Library.Forms.UserList"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Library.Forms"
        mc:Ignorable="d"
        Title="User List" Height="450" Width="640" WindowStartupLocation="CenterScreen" Icon="/Forms/book.png">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                <TextBox x:Name="searchBox" Width="250" Height="28" FontSize="16" VerticalContentAlignment="Center"/>
                <Button x:Name="searchBtn" Content="Search" Width="80" Height="28" Margin="10,0,0,0" Click="searchBtn_Click"/>

                <Label Content="Role:" VerticalAlignment="Center" Margin="20,0,5,0"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="5,5,0,0">
                <RadioButton x:Name="rbAll" Content="All" IsChecked="True" VerticalAlignment="Center" Margin="0,0,15,0"/>
                <RadioButton x:Name="rbFirstName" Content="First name" VerticalAlignment="Center" Margin="0,0,15,0"/>
                <RadioButton x:Name="rbLastName" Content="Last name" VerticalAlignment="Center" Margin="0,0,15,0"/>
                <RadioButton x:Name="rbUsername" Content="Username" VerticalAlignment="Center"/>
                <ComboBox x:Name="roleComboBox" Width="91" Height="28" VerticalContentAlignment="Center" Margin="60,0,0,0">
                    <ComboBoxItem Content="All" IsSelected="True"/>
                    <ComboBoxItem Content="Reader"/>
                    <ComboBoxItem Content="Librarian"/>
                </ComboBox>
            </StackPanel>
        </StackPanel>

        <DataGrid x:Name="UsersGrid" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True" GridLinesVisibility="Horizontal">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Binding="{Binding IdUser}" Width="50" />
                <DataGridTextColumn Header="Last name" Binding="{Binding LastName}" Width="*" />
                <DataGridTextColumn Header="First name" Binding="{Binding FirstName}" Width="*" />
                <DataGridTextColumn Header="Username" Binding="{Binding Username}" Width="*" />
                <DataGridTextColumn Header="Role" Binding="{Binding Role}" Width="100" />
            </DataGrid.Columns>
        </DataGrid>

        <Grid Grid.Row="2" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button x:Name="backBtn" Grid.Column="0" Content="Back" Width="80" Height="30" Click="backBtn_Click"/>

            <StackPanel Grid.Column="2" Orientation="Horizontal">
                <Button x:Name="moreInfoBtn" Content="More info" Width="90" Height="30" Margin="0,0,8,0" Click="moreInfoBtn_Click"/>
                <Button x:Name="editUserBtn" Content="Edit user" Width="90" Height="30" Margin="0,0,8,0" Click="editUserBtn_Click"/>
                <Button x:Name="addUserBtn" Content="Add user" Width="90" Height="30" Margin="0,0,8,0" Click="addUserBtn_Click"/>
                <Button x:Name="deleteUserBtn" Content="Delete" Width="90" Height="30" Click="deleteUserBtn_Click"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>